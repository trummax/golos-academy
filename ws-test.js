const WebSocket = require('ws');
/*

getCommits(cfrepo)
		.then(commits => {
			dsdsds
		.then(commits => {
			commits = commits.filter(commit => commit.commit.message.search(/Add/)!== -1)
			if(commits[0].sha!==lastCommitSHA) {
				lastCommitSHA = commits[0].sha
				let projectName = commits[0].message.search(/Add/)
				if(projectName!==-1) {
					projectName = projectName.match(/Add: (.*)/)[1]
				}
				readFile(dataRepo)
					.then(res=>)
			}
			else 
		})*/